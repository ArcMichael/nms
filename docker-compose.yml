version: '3' # docker-compose -f ./docker-compose.dev.yml

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4100:4100"
    environment:
      - "NODE_ENV=production"
    container_name: next